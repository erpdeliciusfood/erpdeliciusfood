import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.emeraldfalconplay',
  appName: 'emerald-falcon-play',
  webDir: 'dist'
};

export default config;
